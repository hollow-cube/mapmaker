# compile
This module is a java compiler plugin which provides some utilities, mostly for debugging.
There is a hard dependency on the common module, which has some annotations referenced by this tool.

* Inserts thread checks for async calls
