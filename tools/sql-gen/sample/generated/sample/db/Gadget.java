// Generated by sql-gen from src/main/sql. Do not edit.
package sample.db;

import java.math.BigDecimal;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.time.Instant;
import java.time.LocalDate;
import java.util.List;
import java.util.UUID;
import net.hollowcube.sqlgen.runtime.Jdbc;
import org.jetbrains.annotations.Nullable;

/**
 * A row of the `gadget` table.
 */
public record Gadget(UUID id, GadgetStatus status, String label, int quantity, double weight,
        BigDecimal price, byte[] payload, String metadata, Instant createdAt, LocalDate releasedOn,
        List<String> tags, List<Integer> ranks, @Nullable String note,
        @Nullable Instant retiredAt) {
    /**
     * Reads one `gadget` row, its first column at `col`.
     */
    static Gadget read(ResultSet rs, int col) throws SQLException {
        return new Gadget(
                rs.getObject(col, UUID.class),
                GadgetStatus.fromPg(rs.getString(col + 1)),
                rs.getString(col + 2),
                rs.getInt(col + 3),
                rs.getDouble(col + 4),
                rs.getBigDecimal(col + 5),
                rs.getBytes(col + 6),
                rs.getString(col + 7),
                Jdbc.getInstant(rs, col + 8),
                rs.getObject(col + 9, LocalDate.class),
                Jdbc.getList(rs, col + 10, String.class),
                Jdbc.getList(rs, col + 11, Integer.class),
                rs.getString(col + 12),
                Jdbc.getInstant(rs, col + 13));
    }
}
