// Generated by sql-gen from src/main/sql. Do not edit.
package sample.db;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.ArrayList;
import java.util.List;
import net.hollowcube.sqlgen.runtime.ConnectionSource;
import net.hollowcube.sqlgen.runtime.Sneaky;

/**
 * Runs WidgetsQueries against a connection borrowed per statement.
 */
final class WidgetsQueriesImpl implements WidgetsQueries {
    private static final String LIST_WIDGETS = """
        select widget.*
        from widget
        order by id
        limit ?""";

    private static final String SEARCH_WIDGETS = """
        select widget.*,
               count(*) over () as total_count
        from widget
        where name ilike ?
        limit ? offset ?""";

    private final ConnectionSource source;

    WidgetsQueriesImpl(ConnectionSource source) {
        this.source = source;
    }

    @Override
    public List<Widget> listWidgets(long limit) {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(LIST_WIDGETS)) {
                ps.setLong(1, limit);
                try (ResultSet rs = ps.executeQuery()) {
                    List<Widget> rows = new ArrayList<>();
                    while (rs.next()) rows.add(Widget.read(rs, 1));
                    return rows;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Override
    public List<WidgetsQueries.SearchWidgetsRow> searchWidgets(String query, long limit,
            long offset) {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(SEARCH_WIDGETS)) {
                ps.setString(1, query);
                ps.setLong(2, limit);
                ps.setLong(3, offset);
                try (ResultSet rs = ps.executeQuery()) {
                    List<WidgetsQueries.SearchWidgetsRow> rows = new ArrayList<>();
                    while (rs.next()) rows.add(new WidgetsQueries.SearchWidgetsRow(Widget.read(rs, 1), rs.getLong(5)));
                    return rows;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }
}
