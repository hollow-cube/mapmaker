// Generated by sql-gen from src/main/sql. Do not edit.
package sample.db;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.UUID;
import org.jetbrains.annotations.Nullable;

/**
 * A row of the `gadget_review` table.
 */
public record GadgetReview(int id, UUID gadgetId, int stars) {
    /**
     * Reads one `gadget_review` row, its first column at `col`.
     */
    static GadgetReview read(ResultSet rs, int col) throws SQLException {
        return new GadgetReview(
                rs.getInt(col),
                rs.getObject(col + 1, UUID.class),
                rs.getInt(col + 2));
    }

    /**
     * As {@link #read}, for a row that an outer join may not have produced.
     */
    @Nullable
    static GadgetReview readOrNull(ResultSet rs, int col) throws SQLException {
        if (rs.getObject(col) == null) return null;
        return read(rs, col);
    }
}
