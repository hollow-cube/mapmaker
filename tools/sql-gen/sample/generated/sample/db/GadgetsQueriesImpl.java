// Generated by sql-gen from src/main/sql. Do not edit.
package sample.db;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.ArrayList;
import java.util.List;
import java.util.UUID;
import net.hollowcube.sqlgen.runtime.ConnectionSource;
import net.hollowcube.sqlgen.runtime.Jdbc;
import net.hollowcube.sqlgen.runtime.Sneaky;
import net.hollowcube.sqlgen.runtime.SqlFragment;
import org.jetbrains.annotations.Nullable;

/**
 * Runs GadgetsQueries against a connection borrowed per statement.
 */
final class GadgetsQueriesImpl implements GadgetsQueries {
    private static final String INSERT_GADGET = """
        insert into gadget (id, status, label, quantity, weight, price, payload, metadata, created_at,
                            released_on, tags, ranks)
        values (?, ?, ?, ?, ?, ?, ?, ?, ?,
                ?, ?, ?)""";

    private static final String GET_GADGET = """
        select gadget.*
        from gadget
        where id = ?""";

    private static final String COUNT_GADGETS = """
        select count(*)
        from gadget""";

    private static final String LIST_GADGET_LABELS = """
        select label
        from gadget
        order by label""";

    private static final String FIND_GADGETS_BY_TEXT = """
        select gadget.*
        from gadget
        where label = ?
           or note = ?
        order by id""";

    private static final String LIST_GADGETS_WITH_STARS = """
        select gadget.*, review.stars
        from gadget
                 left join gadget_review review on review.gadget_id = gadget.id
        order by gadget.id""";

    private static final String LIST_GADGETS_WITH_REVIEW = """
        select gadget.*, review.*
        from gadget
                 left join gadget_review review on review.gadget_id = gadget.id
        order by gadget.id""";

    private static final String PAIR_GADGETS = """
        select a.*, b.*
        from gadget a
                 join gadget b on b.label = a.label and b.id > a.id
        order by a.id, b.id""";

    private static final String INSERT_REVIEW = """
        insert into gadget_review (id, gadget_id, stars)
        values (?, ?, ?)
        returning gadget_review.*""";

    private static final String DELETE_GADGET = """
        delete
        from gadget
        where id = ?""";

    private static final String SEARCH_GADGETS_0 = """
        select gadget.*,
               count(*) over ()                     as total_count,
               left(gadget.label, ?)    as label_prefix
        from gadget""";

    private static final String SEARCH_GADGETS_1 = "\n";

    private static final String SEARCH_GADGETS_2 = """

        limit ? offset ?""";

    private final ConnectionSource source;

    GadgetsQueriesImpl(ConnectionSource source) {
        this.source = source;
    }

    @Override
    public long insertGadget(GadgetsQueries.InsertGadgetParams params) {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(INSERT_GADGET)) {
                ps.setObject(1, params.id());
                Jdbc.setInferred(ps, 2, params.status().pgLabel());
                ps.setString(3, params.label());
                ps.setInt(4, params.quantity());
                ps.setDouble(5, params.weight());
                ps.setBigDecimal(6, params.price());
                ps.setBytes(7, params.payload());
                Jdbc.setInferred(ps, 8, params.metadata());
                Jdbc.setInstant(ps, 9, params.createdAt());
                ps.setObject(10, params.releasedOn());
                Jdbc.setList(ps, 11, "varchar", params.tags());
                Jdbc.setList(ps, 12, "int4", params.ranks());
                return ps.executeLargeUpdate();
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Nullable
    @Override
    public Gadget getGadget(UUID id) {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(GET_GADGET)) {
                ps.setObject(1, id);
                try (ResultSet rs = ps.executeQuery()) {
                    return rs.next() ? Gadget.read(rs, 1) : null;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Override
    public long countGadgets() {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(COUNT_GADGETS)) {
                try (ResultSet rs = ps.executeQuery()) {
                    if (!rs.next()) throw new SQLException("countGadgets returned no row");
                    return rs.getLong(1);
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Override
    public List<String> listGadgetLabels() {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(LIST_GADGET_LABELS)) {
                try (ResultSet rs = ps.executeQuery()) {
                    List<String> rows = new ArrayList<>();
                    while (rs.next()) rows.add(rs.getString(1));
                    return rows;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Override
    public List<Gadget> findGadgetsByText(String needle) {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(FIND_GADGETS_BY_TEXT)) {
                ps.setString(1, needle);
                ps.setString(2, needle);
                try (ResultSet rs = ps.executeQuery()) {
                    List<Gadget> rows = new ArrayList<>();
                    while (rs.next()) rows.add(Gadget.read(rs, 1));
                    return rows;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Override
    public List<GadgetsQueries.ListGadgetsWithStarsRow> listGadgetsWithStars() {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(LIST_GADGETS_WITH_STARS)) {
                try (ResultSet rs = ps.executeQuery()) {
                    List<GadgetsQueries.ListGadgetsWithStarsRow> rows = new ArrayList<>();
                    while (rs.next()) rows.add(new GadgetsQueries.ListGadgetsWithStarsRow(Gadget.read(rs, 1), rs.getObject(15, Integer.class)));
                    return rows;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Override
    public List<GadgetsQueries.ListGadgetsWithReviewRow> listGadgetsWithReview() {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(LIST_GADGETS_WITH_REVIEW)) {
                try (ResultSet rs = ps.executeQuery()) {
                    List<GadgetsQueries.ListGadgetsWithReviewRow> rows = new ArrayList<>();
                    while (rs.next()) rows.add(new GadgetsQueries.ListGadgetsWithReviewRow(Gadget.read(rs, 1), GadgetReview.readOrNull(rs, 15)));
                    return rows;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Override
    public List<GadgetsQueries.PairGadgetsRow> pairGadgets() {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(PAIR_GADGETS)) {
                try (ResultSet rs = ps.executeQuery()) {
                    List<GadgetsQueries.PairGadgetsRow> rows = new ArrayList<>();
                    while (rs.next()) rows.add(new GadgetsQueries.PairGadgetsRow(Gadget.read(rs, 1), Gadget.read(rs, 15)));
                    return rows;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Nullable
    @Override
    public GadgetReview insertReview(int id, UUID gadgetId, int stars) {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(INSERT_REVIEW)) {
                ps.setInt(1, id);
                ps.setObject(2, gadgetId);
                ps.setInt(3, stars);
                try (ResultSet rs = ps.executeQuery()) {
                    return rs.next() ? GadgetReview.read(rs, 1) : null;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Override
    public long deleteGadget(UUID id) {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(DELETE_GADGET)) {
                ps.setObject(1, id);
                return ps.executeLargeUpdate();
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Override
    public List<GadgetsQueries.SearchGadgetsRow> searchGadgets(int prefixLength, long limit,
            long offset, @Nullable SqlFragment where, @Nullable SqlFragment orderBy) {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(SEARCH_GADGETS_0
                    + SqlFragment.clause("where", where)
                    + SEARCH_GADGETS_1
                    + SqlFragment.clause("order by", orderBy)
                    + SEARCH_GADGETS_2)) {
                int i = 1;
                ps.setInt(i++, prefixLength);
                i = SqlFragment.bind(ps, i, where);
                i = SqlFragment.bind(ps, i, orderBy);
                ps.setLong(i++, limit);
                ps.setLong(i++, offset);
                try (ResultSet rs = ps.executeQuery()) {
                    List<GadgetsQueries.SearchGadgetsRow> rows = new ArrayList<>();
                    while (rs.next()) rows.add(new GadgetsQueries.SearchGadgetsRow(Gadget.read(rs, 1), rs.getLong(15), rs.getString(16)));
                    return rows;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }
}
