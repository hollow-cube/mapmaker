// Generated by sql-gen from src/main/sql. Do not edit.
package sample.db;

import java.util.ArrayList;
import java.util.List;
import org.jetbrains.annotations.Nullable;

/**
 * The `gadget_status` Postgres enum.
 */
public enum GadgetStatus {
    DRAFT("draft"),

    ACTIVE("active"),

    RETIRED("retired");

    private final String pgLabel;

    GadgetStatus(String pgLabel) {
        this.pgLabel = pgLabel;
    }

    /**
     * The label this constant is stored as.
     */
    public String pgLabel() {
        return pgLabel;
    }

    @Nullable
    public static GadgetStatus fromPg(@Nullable String label) {
        if (label == null) return null;
        for (GadgetStatus value : values()) {
            if (value.pgLabel.equals(label)) return value;
        }
        throw new IllegalArgumentException("unknown gadget_status label: " + label);
    }

    @Nullable
    public static List<GadgetStatus> listFromPg(@Nullable List<String> labels) {
        if (labels == null) return null;
        List<GadgetStatus> values = new ArrayList<>(labels.size());
        for (String label : labels) values.add(fromPg(label));
        return values;
    }

    @Nullable
    public static List<String> pgLabels(@Nullable List<GadgetStatus> values) {
        if (values == null) return null;
        List<String> labels = new ArrayList<>(values.size());
        for (GadgetStatus value : values) labels.add(value == null ? null : value.pgLabel);
        return labels;
    }
}
