#name: pr-isolate.yml
#on:
#  pull_request:
#    branches: [ main ]
#
#jobs:
#  docker-build-map-isolate:
#    runs-on: blacksmith-16vcpu-ubuntu-2404
#    steps:
#      - name: Import Secrets
#        uses: hollow-cube/actions/secrets@main
#        with:
#          token: ${{ secrets.VAULT_TOKEN }}
#          path: global/github
#
#      - uses: actions/checkout@v4
#        with:
#          submodules: 'recursive'
#      - uses: actions/setup-java@v5
#        with:
#          java-version: '25'
#          distribution: 'graalvm'
#      - name: Setup Gradle
#        uses: gradle/actions/setup-gradle@v4
#        with:
#          cache-read-only: true
#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v2
#      - name: Login to Docker Hub
#        uses: docker/login-action@v2
#        with:
#          username: mworzala
#          password: ${{ env.DOCKER_PASS }}
#
#      - name: Build resource pack
#        id: build-rp
#        run: |
#          ./gradlew :bin:packer:buildClient
#
#          export RESOURCE_PACK_SHA=$(sha1sum build/client.zip | cut -d ' ' -f1)
#          echo "resource_pack_sha=${RESOURCE_PACK_SHA}" >> "$GITHUB_OUTPUT"
#          echo "Resource Pack Hash: ${RESOURCE_PACK_SHA}" >> "$GITHUB_STEP_SUMMARY"
#
#          mkdir -p out/resource-pack
#          cp build/client.zip out/resource-pack/${RESOURCE_PACK_SHA}.zip
#      - name: Upload resource pack
#        uses: jakejarvis/s3-sync-action@master
#        env:
#          AWS_S3_ENDPOINT: https://bdb9cb2904188b760591dc1589a1ccf3.r2.cloudflarestorage.com
#          AWS_S3_BUCKET: assets
#          AWS_ACCESS_KEY_ID: ${{ env.R2_ASSETS_ACCESS_KEY }}
#          AWS_SECRET_ACCESS_KEY: ${{ env.R2_ASSETS_SECRET_KEY }}
#          AWS_S3_REGION: "auto"
#          SOURCE_DIR: out
#
#      - name: Build map-isolate native image
#        env:
#          MAPMAKER_VERSION: ${{ github.event.pull_request.title || github.ref_name }}
#          MAPMAKER_COMMIT_HASH: ${{ github.event.pull_request.head.sha || github.sha }}
#          MAPMAKER_RESOURCE_PACK_HASH: ${{ steps.build-rp.outputs.resource_pack_sha }}
#        run: ./gradlew :bin:map-isolate:nativeCompile -PisRelease=false --no-configuration-cache
#      - name: Upload build report
#        uses: actions/upload-artifact@v4
#        with:
#          name: native-image-build-report
#          path: bin/map-isolate/build/native/nativeCompile/map-isolate-build-report.html
#      - name: Link build report in summary
#        run: echo "📊 [Native image build report](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}#artifacts) uploaded as artifact" >> "$GITHUB_STEP_SUMMARY"
#      - name: Build & push docker image
#        uses: docker/build-push-action@v3
#        with:
#          context: .
#          file: "bin/map-isolate/deploy/Dockerfile-native"
#          platforms: linux/amd64
#          push: true
#          tags: mworzala/mapmaker-map-isolate:preview-${{ github.event.pull_request.head.sha || github.sha }}
