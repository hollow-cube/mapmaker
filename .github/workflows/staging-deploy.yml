name: Staging Build & Deploy
concurrency: Staging

on:
  push:
    branches: [ main ]

jobs:
  resource-pack-build:
    runs-on: ubuntu-latest
    outputs:
      resource_pack_sha: ${{ steps.build.outputs.resource_pack_sha }}
    steps:
      - name: Import Secrets
        uses: hollow-cube/actions/secrets@main
        with:
          token: ${{ secrets.VAULT_TOKEN }}
          path: global/resourcepackr2

      - uses: actions/checkout@v3
      - name: Cache Bazel
        uses: actions/cache@v3
        with:
          path: |
            ~/.cache/bazel
          key: ${{ runner.os }}-bazel-rp-${{ hashFiles('.bazelversion', '.bazelrc', 'WORKSPACE', 'WORKSPACE.bazel', 'MODULE.bazel') }}
          restore-keys: |
            ${{ runner.os }}-bazel-rp-

      - name: Build resource pack
        id: build
        run: |
          bazel build //:packer_development_client
          echo "resource_pack_sha=$(sha1sum bazel-bin/client.zip | cut -d ' ' -f1)" >> "$GITHUB_OUTPUT"
          
          mkdir -p out
          cp bazel-bin/client.zip out/mapmaker-${{ github.sha }}.zip
      - name: Upload resource pack
        uses: jakejarvis/s3-sync-action@master
        env:
          AWS_S3_BUCKET: resource-pack
          AWS_ACCESS_KEY_ID: ${{ env.RP_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ env.RP_SECRET_KEY }}
          AWS_S3_ENDPOINT: "https://bdb9cb2904188b760591dc1589a1ccf3.r2.cloudflarestorage.com"
          SOURCE_DIR: out

  docker-build:
    runs-on: ubuntu-latest
    steps:
      - name: Import Secrets
        uses: hollow-cube/actions/secrets@main
        with:
          token: ${{ secrets.VAULT_TOKEN }}
          path: global/github

      - uses: actions/checkout@v3
      - name: Cache Bazel
        uses: actions/cache@v3
        with:
          path: |
            ~/.cache/bazel
          key: ${{ runner.os }}-bazel-bds-${{ hashFiles('.bazelversion', '.bazelrc', 'WORKSPACE', 'WORKSPACE.bazel', 'MODULE.bazel') }}
          restore-keys: |
            ${{ runner.os }}-bazel-bds-

      # Build server
      - name: Build standalone binary
        run: bazel build //:development_deploy.jar

      # Build and push docker image
      - run: cp bazel-bin/development_deploy.jar development-all.jar
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: mworzala
          password: ${{ env.DOCKER_PASS }}
      - name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: .
          file: "bin/development/deploy/Dockerfile"
          platforms: linux/amd64
          push: true
          tags: mworzala/mapmaker-dev:${{ github.sha }}
  helm-build:
    runs-on: ubuntu-latest
    steps:
      - name: Helm build
        uses: hollow-cube/actions/helm-build@main
        with:
          vault-token: ${{ secrets.VAULT_TOKEN }}
          name: mapmaker-dev
          path: bin/development/deploy/mapmaker-dev
  helm-deploy:
    runs-on: 'ubuntu-latest'
    needs:
      - docker-build
      - helm-build
      - resource-pack-build
    steps:
      - name: Import Secrets
        uses: hollow-cube/actions/secrets@main
        with:
          token: ${{ secrets.VAULT_TOKEN }}
          path: global/github
      - name: Tailscale
        uses: tailscale/github-action@main
        with:
          oauth-client-id: ${{ env.TAILSCALE_OAUTH_KEY }}
          oauth-secret: ${{ env.TAILSCALE_OAUTH_SECRET }}
          tags: tag:ci
          version: 1.52.0
      - uses: azure/setup-helm@v3
        with:
          version: v3.10.2
      - uses: actions/checkout@v3

      # <ALERT BEEP BOOP DIFFERENT FROM COMMON ACTION>
      - name: Import Server Secrets
        uses: hollow-cube/actions/secrets@main
        with:
          token: ${{ secrets.VAULT_TOKEN }}
          path: staging/mapmaker
      # </ALERT>
      - name: Upgrade/install helm chart
        env:
          COMMIT_SHA: ${{ github.sha }}
          RESOURCE_PACK_HASH: ${{ needs.resource-pack-build.outputs.resource_pack_sha }}
        run: |
          # Setup Kubernetes
          tailscale configure kubeconfig ovh-staging
          kubectl cluster-info

          # Deploy the chart
          helm registry login registry-1.docker.io --username mworzala --password ${{ env.DOCKER_PASS }}
          helm pull oci://registry-1.docker.io/mworzala/mapmaker-dev --version "1.0.0-${{ github.sha }}"
          
          # <ALERT BEEP BOOP DIFFERENT FROM COMMON ACTION>
          cat bin/development/deploy/mapmaker-dev/values-staging.yaml | envsubst > ./values-override.yaml
          # </ALERT>
          
          helm upgrade mapmaker-dev mapmaker-dev-1.0.0-${{ github.sha }}.tgz \
               --install --create-namespace --namespace mapmaker \
               --reset-values -f ./values-override.yaml \
               --set base.image.tag="${{ github.sha }}" 
