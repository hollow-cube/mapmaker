# Default values for mapmaker-dev.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: registry.hollowcube.net/mapmaker-dev
  pullPolicy: Always

env:
  # todo obviously very insecure, need to replace all of these with secrets & probably some secret manager like Vault.
  #      then delete all of these existing credentials
  MAPMAKER_MONGO_URI: "mongodb+srv://admin:37nk6wH1j2XjSZnq@mapmaker.xxj8xag.mongodb.net/"
  # Legacy, kept for chat facet which does not use config quite yet
  MM_MONGO_URI: "mongodb+srv://admin:37nk6wH1j2XjSZnq@mapmaker.xxj8xag.mongodb.net/"
  MAPMAKER_S3_ADDRESS: "https://minio.dev.hollowcube.net/"
  MAPMAKER_S3_ACCESSKEY: "Ai65UJERihroEJTi"
  MAPMAKER_S3_SECRETKEY: "fzp8s99MvaZojs3BIpPLVNcc3dfpvPDv"
  MAPMAKER_SPICEDB_ADDRESS: "grpc.authzed.com:443"
  MAPMAKER_SPICEDB_SECRETKEY: "tc_mapmaker_dev_k8s_feb473a28a9f019649d29a209406219203c6619cbc878465af9065ae95001e70b9444eabf84c19130a8ad3ae9a8c9e28fa0ce492b32b13f44f102a6d23ea6ee6"
  MAPMAKER_SPICEDB_TLS: "true"
  PROMETHEUS_DISABLE_CREATED_SERIES: "true"

imagePullSecrets:
  - name: regcred
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}
