replicaCount: 1

env:
  MAPMAKER_MONGO_URI: "${MONGO_URI}"
  # Legacy, kept for chat facet which does not use config quite yet
  MM_MONGO_URI: "${MONGO_URI}"
  MAPMAKER_S3_URI: "s3://${R2_ACCESS_KEY}:${R2_SECRET_KEY}@bdb9cb2904188b760591dc1589a1ccf3.r2.cloudflarestorage.com/mapmaker"
  MAPMAKER_SPICEDB_ADDRESS: "staging-spicedb:50051"
  MAPMAKER_SPICEDB_SECRETKEY: "${SPICEDB_SECRET_KEY}"
  MAPMAKER_SPICEDB_TLS: "false"
  MAPMAKER_KAFKA_BOOTSTRAP_SERVERS: "staging-kafka-bootstrap:9092"
  MAPMAKER_PYROSCOPE_ENDPOINT: "http://pyroscope.mgmt.svc.cluster.local:4040"
