apiVersion: v1
kind: Service
metadata:
  name: {{ include "map-isolate.fullname" . }}
  labels:
    {{- include "map-isolate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "map-isolate.selectorLabels" . | nindent 4 }}
