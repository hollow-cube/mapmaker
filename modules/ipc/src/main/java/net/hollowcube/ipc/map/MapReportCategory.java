package net.hollowcube.ipc.map;

/// Stored by ordinal, so only ever appended to.
public enum MapReportCategory {
    CHEATED,
    DISCRIMINATION,
    EXPLICIT_CONTENT,
    SPAM,
    DMCA,
    TROLL,
    UNPLAYABLE,
    UNKNOWN,
}
