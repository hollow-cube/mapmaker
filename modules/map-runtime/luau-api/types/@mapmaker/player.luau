-- Generated by lua-slopgen. DO NOT EDIT.

local Entity = require("./entity")
local Item = require("./item")

type Player_Fields = {
	read uuid: string,
	read name: string,
	read position: vector,
	read yaw: number,
	read pitch: number,
	read sidebar: Sidebar,
	read world: WorldView,
	read deprecated_scale: number,
	read on_hit_player: EventSource<Player>,
	read on_interact_block: EventSource<vector>,
	read on_jump: EventSource<()>,
	read on_use_item: EventSource<()>,
	read send_message: (self: Player, message: AnyText) -> (),
	read show_title: (self: Player, title: AnyText, subtitle: AnyText?) -> (),
	read play_sound: (self: Player, sound: string, options: { volume: number?, pitch: number?, category: string? }?) -> (),
	read play_sound_at: (self: Player, sound: string, position: vector, options: { volume: number?, pitch: number?, category: string? }?) -> (),
	read stop_sound: (self: Player, sound: string?, category: string?) -> (),
	read teleport: (self: Player, position: vector, yaw: number?, pitch: number?, relative: string?) -> (),
	read get_slot: (self: Player, slot: string) -> Item.Item,
	read set_slot: (self: Player, slot: string, item: Item.Item) -> (),
	read set_item: (self: Player, slot: number, item: Item.Item) -> (),
	read add_effect: (self: Player, effect: string, amplifier: number?, duration: number?) -> (),
	read remove_effect: (self: Player, effect: string) -> (),
	read clear_effects: (self: Player) -> (),
}
type Player_Meta = {
	__eq: (self: Player) -> boolean,
}
export type Player = typeof(setmetatable({} :: Player_Fields, {} :: Player_Meta))

export type WorldView = {
	read spawn_text_prop: (self: WorldView, init: { scale: vector?, billboard: ("fixed" | "vertical" | "horizontal" | "center")?, text: AnyText?, shadow: boolean?, background: number?, opacity: number? } & { position: vector, yaw: number?, pitch: number? }) -> Entity.TextProp,
	read set_block: (self: WorldView, position: vector, blockState: string) -> (),
}

return {}
