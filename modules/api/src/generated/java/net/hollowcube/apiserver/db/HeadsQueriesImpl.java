// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.ArrayList;
import java.util.List;
import net.hollowcube.sqlgen.runtime.ConnectionSource;
import net.hollowcube.sqlgen.runtime.Sneaky;

/**
 * Runs HeadsQueries against a connection borrowed per statement.
 */
final class HeadsQueriesImpl implements HeadsQueries {
    private static final String GET_RANDOM_HEADS = """
        select head_db.*,
               count(*) over () as total_count
        from head_db
        order by random()
        limit ?""";

    private static final String GET_HEADS_WITH_SEARCH = """
        select head_db.*,
               count(*) over () as total_count
        from head_db
        where head_db_search(name, tags) @@ to_tsquery('simple', ?)
        order by ts_rank(head_db_search(name, tags), to_tsquery('simple', ?)) desc, head_db.id
        limit ? offset ?""";

    private static final String GET_HEADS_WITH_CATEGORY = """
        select head_db.*,
               count(*) over () as total_count
        from head_db
        where category = ?
        order by head_db.name, head_db.id
        limit ? offset ?""";

    private final ConnectionSource source;

    HeadsQueriesImpl(ConnectionSource source) {
        this.source = source;
    }

    @Override
    public List<HeadsQueries.GetRandomHeadsRow> getRandomHeads(long limit) {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(GET_RANDOM_HEADS)) {
                ps.setLong(1, limit);
                try (ResultSet rs = ps.executeQuery()) {
                    List<HeadsQueries.GetRandomHeadsRow> rows = new ArrayList<>();
                    while (rs.next()) rows.add(new HeadsQueries.GetRandomHeadsRow(HeadDb.read(rs, 1), rs.getLong(6)));
                    return rows;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Override
    public List<HeadsQueries.GetHeadsWithSearchRow> getHeadsWithSearch(String query, long limit,
            long offset) {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(GET_HEADS_WITH_SEARCH)) {
                ps.setString(1, query);
                ps.setString(2, query);
                ps.setLong(3, limit);
                ps.setLong(4, offset);
                try (ResultSet rs = ps.executeQuery()) {
                    List<HeadsQueries.GetHeadsWithSearchRow> rows = new ArrayList<>();
                    while (rs.next()) rows.add(new HeadsQueries.GetHeadsWithSearchRow(HeadDb.read(rs, 1), rs.getLong(6)));
                    return rows;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Override
    public List<HeadsQueries.GetHeadsWithCategoryRow> getHeadsWithCategory(String category,
            long limit, long offset) {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(GET_HEADS_WITH_CATEGORY)) {
                ps.setString(1, category);
                ps.setLong(2, limit);
                ps.setLong(3, offset);
                try (ResultSet rs = ps.executeQuery()) {
                    List<HeadsQueries.GetHeadsWithCategoryRow> rows = new ArrayList<>();
                    while (rs.next()) rows.add(new HeadsQueries.GetHeadsWithCategoryRow(HeadDb.read(rs, 1), rs.getLong(6)));
                    return rows;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }
}
