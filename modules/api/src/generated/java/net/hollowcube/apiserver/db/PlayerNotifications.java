// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.time.Instant;
import java.util.UUID;
import net.hollowcube.sqlgen.runtime.Jdbc;
import org.jetbrains.annotations.Nullable;

/**
 * A row of the `player_notifications` table.
 */
public record PlayerNotifications(UUID id, UUID playerId, String type, String key,
        @Nullable String data, Instant createdAt, @Nullable Instant readAt,
        @Nullable Instant expiresAt, @Nullable Instant deletedAt) {
    /**
     * Reads one `player_notifications` row, its first column at `col`.
     */
    static PlayerNotifications read(ResultSet rs, int col) throws SQLException {
        return new PlayerNotifications(
                rs.getObject(col, UUID.class),
                rs.getObject(col + 1, UUID.class),
                rs.getString(col + 2),
                rs.getString(col + 3),
                rs.getString(col + 4),
                Jdbc.getInstant(rs, col + 5),
                Jdbc.getInstant(rs, col + 6),
                Jdbc.getInstant(rs, col + 7),
                Jdbc.getInstant(rs, col + 8));
    }
}
