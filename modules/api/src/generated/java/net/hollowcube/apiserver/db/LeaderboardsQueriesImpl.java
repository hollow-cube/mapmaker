// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.ArrayList;
import java.util.List;
import java.util.UUID;
import net.hollowcube.sqlgen.runtime.ConnectionSource;
import net.hollowcube.sqlgen.runtime.Sneaky;

/**
 * Runs LeaderboardsQueries against a connection borrowed per statement.
 */
@SuppressWarnings("UnusedReturnValue")
final class LeaderboardsQueriesImpl implements LeaderboardsQueries {
    private static final String TOP_TIMES = """
        -- The players holding the fastest time on the most published, ascending time boards.
        with shortest_playtimes as (select map_id,
                                           min((greatest((playtime + 25) / 50, ticks) * 50)::bigint) as min_playtime
                                    from save_states
                                             join maps on save_states.map_id = maps.id
                                    where deleted is null
                                      and completed
                                      and playtime != 0
                                      and type in ('playing', 'verifying')
                                      and maps.published_at is not null
                                      and maps.deleted_at is null
                                      and coalesce(maps.leaderboard ->> 'format', 'time') = 'time'
                                      and coalesce(maps.leaderboard ->> 'asc', 'true') = 'true'
                                    group by map_id)
        select s1.player_id, count(distinct s1.map_id) as top_times
        from shortest_playtimes
                 join save_states s1
                      on s1.map_id = shortest_playtimes.map_id
                          and (greatest((s1.playtime + 25) / 50, s1.ticks) * 50)::bigint = shortest_playtimes.min_playtime
        where s1.deleted is null
          and s1.completed
        group by s1.player_id
        order by top_times desc
        limit 10""";

    private static final String TOP_TIMES_FOR_PLAYER = """
        select count(distinct s1.map_id)::int as top_times
        from save_states s1
                 join maps on s1.map_id = maps.id
        where s1.player_id = ?
          and s1.deleted is null
          and s1.completed
          and s1.playtime != 0
          and s1.type in ('playing', 'verifying')
          and maps.published_at is not null
          and maps.deleted_at is null
          and coalesce(maps.leaderboard ->> 'format', 'time') = 'time'
          and coalesce(maps.leaderboard ->> 'asc', 'true') = 'true'
          and not exists (select 1
                          from save_states s2
                          where s2.map_id = s1.map_id
                            and s2.deleted is null
                            and s2.completed
                            and s2.playtime != 0
                            and s2.type in ('playing', 'verifying')
                            and (greatest((s2.playtime + 25) / 50, s2.ticks) * 50)::bigint
                              < (greatest((s1.playtime + 25) / 50, s1.ticks) * 50)::bigint)""";

    private static final String MAPS_BEATEN = """
        select s1.player_id, count(distinct s1.map_id) as maps_beaten
        from save_states s1
                 join maps on s1.map_id = maps.id
        where s1.deleted is null
          and s1.completed
          and s1.type in ('playing', 'verifying')
          and maps.published_at is not null
        group by s1.player_id
        order by maps_beaten desc
        limit 10""";

    private static final String MAPS_BEATEN_FOR_PLAYER = """
        select count(distinct map_id)::int as maps_beaten
        from save_states
                 join maps on save_states.map_id = maps.id
        where deleted is null
          and completed
          and type in ('playing', 'verifying')
          and player_id = ?
          and maps.published_at is not null""";

    private static final String PLAYER_BEST_TIMES = """
        -- The player's fastest run on every published map they have finished, by playtime rather than by
        -- score, which is what the top-times rank is measured against.
        select distinct on (save_states.map_id) save_states.map_id,
                                                save_states.playtime,
                                                maps.opt_name as map_name,
                                                maps.published_id
        from save_states
                 join maps on save_states.map_id = maps.id
        where save_states.deleted is null
          and save_states.completed
          and save_states.player_id = ?
          and save_states.type in ('playing', 'verifying')
          and save_states.playtime != 0
          and maps.published_at is not null
          and maps.deleted_at is null
        order by save_states.map_id, save_states.playtime""";

    private final ConnectionSource source;

    LeaderboardsQueriesImpl(ConnectionSource source) {
        this.source = source;
    }

    @Override
    public List<LeaderboardsQueries.TopTimesRow> topTimes() {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(TOP_TIMES)) {
                try (ResultSet rs = ps.executeQuery()) {
                    List<LeaderboardsQueries.TopTimesRow> rows = new ArrayList<>();
                    while (rs.next()) rows.add(new LeaderboardsQueries.TopTimesRow(rs.getObject(1, UUID.class), rs.getLong(2)));
                    return rows;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Override
    public int topTimesForPlayer(UUID playerId) {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(TOP_TIMES_FOR_PLAYER)) {
                ps.setObject(1, playerId);
                try (ResultSet rs = ps.executeQuery()) {
                    if (!rs.next()) throw new SQLException("topTimesForPlayer returned no row");
                    return rs.getInt(1);
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Override
    public List<LeaderboardsQueries.MapsBeatenRow> mapsBeaten() {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(MAPS_BEATEN)) {
                try (ResultSet rs = ps.executeQuery()) {
                    List<LeaderboardsQueries.MapsBeatenRow> rows = new ArrayList<>();
                    while (rs.next()) rows.add(new LeaderboardsQueries.MapsBeatenRow(rs.getObject(1, UUID.class), rs.getLong(2)));
                    return rows;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Override
    public int mapsBeatenForPlayer(UUID playerId) {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(MAPS_BEATEN_FOR_PLAYER)) {
                ps.setObject(1, playerId);
                try (ResultSet rs = ps.executeQuery()) {
                    if (!rs.next()) throw new SQLException("mapsBeatenForPlayer returned no row");
                    return rs.getInt(1);
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }

    @Override
    public List<LeaderboardsQueries.PlayerBestTimesRow> playerBestTimes(UUID playerId) {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(PLAYER_BEST_TIMES)) {
                ps.setObject(1, playerId);
                try (ResultSet rs = ps.executeQuery()) {
                    List<LeaderboardsQueries.PlayerBestTimesRow> rows = new ArrayList<>();
                    while (rs.next()) rows.add(new LeaderboardsQueries.PlayerBestTimesRow(rs.getObject(1, UUID.class), rs.getLong(2), rs.getString(3), rs.getObject(4, Long.class)));
                    return rows;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }
}
