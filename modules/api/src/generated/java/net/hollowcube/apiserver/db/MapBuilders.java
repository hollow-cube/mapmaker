// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.time.Instant;
import java.util.UUID;
import net.hollowcube.sqlgen.runtime.Jdbc;
import org.jetbrains.annotations.Nullable;

/**
 * A row of the `map_builders` table.
 */
public record MapBuilders(UUID mapId, UUID playerId, Instant createdAt,
        @Nullable Boolean isPending) {
    /**
     * Reads one `map_builders` row, its first column at `col`.
     */
    static MapBuilders read(ResultSet rs, int col) throws SQLException {
        return new MapBuilders(
                rs.getObject(col, UUID.class),
                rs.getObject(col + 1, UUID.class),
                Jdbc.getInstant(rs, col + 2),
                rs.getObject(col + 3, Boolean.class));
    }
}
