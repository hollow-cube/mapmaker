// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.time.Instant;
import java.util.UUID;
import net.hollowcube.sqlgen.runtime.Jdbc;

/**
 * A row of the `ip_history` table.
 */
public record IpHistory(UUID playerId, String address, Instant firstSeen, Instant lastSeen,
        int seenCount) {
    /**
     * Reads one `ip_history` row, its first column at `col`.
     */
    static IpHistory read(ResultSet rs, int col) throws SQLException {
        return new IpHistory(
                rs.getObject(col, UUID.class),
                rs.getString(col + 1),
                Jdbc.getInstant(rs, col + 2),
                Jdbc.getInstant(rs, col + 3),
                rs.getInt(col + 4));
    }
}
