// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.time.Instant;
import java.util.UUID;
import net.hollowcube.sqlgen.runtime.Jdbc;
import org.jetbrains.annotations.Nullable;

/**
 * A row of the `command_log` table.
 */
public record CommandLog(long id, Instant timestamp, UUID playerId, String serverId,
        @Nullable String mapId, @Nullable String instanceId, String command, boolean remote,
        String outcome, @Nullable String error, int durationMs) {
    /**
     * Reads one `command_log` row, its first column at `col`.
     */
    static CommandLog read(ResultSet rs, int col) throws SQLException {
        return new CommandLog(
                rs.getLong(col),
                Jdbc.getInstant(rs, col + 1),
                rs.getObject(col + 2, UUID.class),
                rs.getString(col + 3),
                rs.getString(col + 4),
                rs.getString(col + 5),
                rs.getString(col + 6),
                rs.getBoolean(col + 7),
                rs.getString(col + 8),
                rs.getString(col + 9),
                rs.getInt(col + 10));
    }
}
