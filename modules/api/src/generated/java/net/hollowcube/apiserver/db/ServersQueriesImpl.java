// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;
import net.hollowcube.sqlgen.runtime.ConnectionSource;
import net.hollowcube.sqlgen.runtime.Sneaky;
import org.jetbrains.annotations.Nullable;

/**
 * Runs ServersQueries against a connection borrowed per statement.
 */
@SuppressWarnings("UnusedReturnValue")
final class ServersQueriesImpl implements ServersQueries {
    private static final String FIND_HUB = """
        -- Ready as of the Go tracker's last pod sync, which runs every 5s.
        select id, cluster_ip, protocol_version
        from server_states
        where role = 'hub'
          and status = 1
          and (?::text is null or id != ?)
        limit 1""";

    private final ConnectionSource source;

    ServersQueriesImpl(ConnectionSource source) {
        this.source = source;
    }

    @Nullable
    @Override
    public ServersQueries.FindHubRow findHub(String exclude) {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(FIND_HUB)) {
                ps.setString(1, exclude);
                ps.setString(2, exclude);
                try (ResultSet rs = ps.executeQuery()) {
                    return rs.next() ? new ServersQueries.FindHubRow(rs.getString(1), rs.getString(2), rs.getInt(3)) : null;
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }
}
