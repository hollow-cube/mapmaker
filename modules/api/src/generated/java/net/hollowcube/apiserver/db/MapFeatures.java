// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.time.Instant;
import java.util.List;
import java.util.UUID;
import net.hollowcube.sqlgen.runtime.Jdbc;
import org.jetbrains.annotations.Nullable;

/**
 * A row of the `map_features` table.
 */
public record MapFeatures(UUID mapId, int featureVersion, Instant indexedAt,
        @Nullable Integer dataVersion, long blockCount, int extentX, int extentY, int extentZ,
        int occupiedCells, int distinctBlocks, double dominantBlockFrac, int entityCount,
        int textDisplayCount, int checkpointCount, @Nullable Double checkpointSpacing,
        int finishCount, int statusCount, List<String> mechanics, List<String> attributes,
        List<String> potionEffects, List<String> settings, int actionCount, int decodeFailures) {
    /**
     * Reads one `map_features` row, its first column at `col`.
     */
    static MapFeatures read(ResultSet rs, int col) throws SQLException {
        return new MapFeatures(
                rs.getObject(col, UUID.class),
                rs.getInt(col + 1),
                Jdbc.getInstant(rs, col + 2),
                rs.getObject(col + 3, Integer.class),
                rs.getLong(col + 4),
                rs.getInt(col + 5),
                rs.getInt(col + 6),
                rs.getInt(col + 7),
                rs.getInt(col + 8),
                rs.getInt(col + 9),
                rs.getDouble(col + 10),
                rs.getInt(col + 11),
                rs.getInt(col + 12),
                rs.getInt(col + 13),
                rs.getObject(col + 14, Double.class),
                rs.getInt(col + 15),
                rs.getInt(col + 16),
                Jdbc.getList(rs, col + 17, String.class),
                Jdbc.getList(rs, col + 18, String.class),
                Jdbc.getList(rs, col + 19, String.class),
                Jdbc.getList(rs, col + 20, String.class),
                rs.getInt(col + 21),
                rs.getInt(col + 22));
    }
}
