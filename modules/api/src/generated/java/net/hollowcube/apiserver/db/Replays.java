// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.time.Instant;
import net.hollowcube.sqlgen.runtime.Jdbc;
import org.jetbrains.annotations.Nullable;

/**
 * A row of the `replays` table.
 */
public record Replays(String id, long version, long recordingRevision, String state,
        String representation, long nextSegmentIndex, byte[] currentPreamble,
        byte[] currentPreambleDigest, @Nullable Long compactedSourceRevision,
        @Nullable String compactedObject, @Nullable Long compactedLength,
        @Nullable byte[] compactedDigest, Instant createdAt, Instant updatedAt,
        @Nullable String outcome) {
    /**
     * Reads one `replays` row, its first column at `col`.
     */
    static Replays read(ResultSet rs, int col) throws SQLException {
        return new Replays(
                rs.getString(col),
                rs.getLong(col + 1),
                rs.getLong(col + 2),
                rs.getString(col + 3),
                rs.getString(col + 4),
                rs.getLong(col + 5),
                rs.getBytes(col + 6),
                rs.getBytes(col + 7),
                rs.getObject(col + 8, Long.class),
                rs.getString(col + 9),
                rs.getObject(col + 10, Long.class),
                rs.getBytes(col + 11),
                Jdbc.getInstant(rs, col + 12),
                Jdbc.getInstant(rs, col + 13),
                rs.getString(col + 14));
    }
}
