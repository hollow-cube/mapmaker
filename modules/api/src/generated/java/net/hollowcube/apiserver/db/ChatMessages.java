// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.time.Instant;
import net.hollowcube.sqlgen.runtime.Jdbc;
import org.jetbrains.annotations.Nullable;

/**
 * A row of the `chat_messages` table.
 */
public record ChatMessages(Instant timestamp, String serverId, String channel, String sender,
        @Nullable String target, String content, @Nullable String censoredBy,
        @Nullable String censoredDetail) {
    /**
     * Reads one `chat_messages` row, its first column at `col`.
     */
    static ChatMessages read(ResultSet rs, int col) throws SQLException {
        return new ChatMessages(
                Jdbc.getInstant(rs, col),
                rs.getString(col + 1),
                rs.getString(col + 2),
                rs.getString(col + 3),
                rs.getString(col + 4),
                rs.getString(col + 5),
                rs.getString(col + 6),
                rs.getString(col + 7));
    }
}
