// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.time.Instant;
import java.util.UUID;
import net.hollowcube.sqlgen.runtime.Jdbc;

/**
 * A row of the `player_blocks` table.
 */
public record PlayerBlocks(UUID playerId, UUID targetId, Instant createdAt) {
    /**
     * Reads one `player_blocks` row, its first column at `col`.
     */
    static PlayerBlocks read(ResultSet rs, int col) throws SQLException {
        return new PlayerBlocks(
                rs.getObject(col, UUID.class),
                rs.getObject(col + 1, UUID.class),
                Jdbc.getInstant(rs, col + 2));
    }
}
