// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.time.Instant;
import java.util.UUID;
import net.hollowcube.sqlgen.runtime.Jdbc;
import org.jetbrains.annotations.Nullable;

/**
 * A row of the `player_sessions` table.
 */
public record PlayerSessions(UUID playerId, Instant createdAt, String proxyId,
        @Nullable String serverId, boolean hidden, @Nullable String username, String skinTexture,
        String skinSignature, @Nullable String pType, @Nullable String pState,
        @Nullable String pInstanceId, @Nullable String pMapId, @Nullable Instant pStartTime,
        @Nullable Instant lastSeen, int protocolVersion) {
    /**
     * Reads one `player_sessions` row, its first column at `col`.
     */
    static PlayerSessions read(ResultSet rs, int col) throws SQLException {
        return new PlayerSessions(
                rs.getObject(col, UUID.class),
                Jdbc.getInstant(rs, col + 1),
                rs.getString(col + 2),
                rs.getString(col + 3),
                rs.getBoolean(col + 4),
                rs.getString(col + 5),
                rs.getString(col + 6),
                rs.getString(col + 7),
                rs.getString(col + 8),
                rs.getString(col + 9),
                rs.getString(col + 10),
                rs.getString(col + 11),
                Jdbc.getInstant(rs, col + 12),
                Jdbc.getInstant(rs, col + 13),
                rs.getInt(col + 14));
    }
}
