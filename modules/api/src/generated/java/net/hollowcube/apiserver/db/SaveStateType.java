// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.util.ArrayList;
import java.util.List;
import org.jetbrains.annotations.Nullable;

/**
 * The `save_state_type` Postgres enum.
 */
public enum SaveStateType {
    EDITING("editing"),

    PLAYING("playing"),

    VERIFYING("verifying");

    private final String pgLabel;

    SaveStateType(String pgLabel) {
        this.pgLabel = pgLabel;
    }

    /**
     * The label this constant is stored as.
     */
    public String pgLabel() {
        return pgLabel;
    }

    @Nullable
    public static SaveStateType fromPg(@Nullable String label) {
        if (label == null) return null;
        for (SaveStateType value : values()) {
            if (value.pgLabel.equals(label)) return value;
        }
        throw new IllegalArgumentException("unknown save_state_type label: " + label);
    }

    @Nullable
    public static List<SaveStateType> listFromPg(@Nullable List<String> labels) {
        if (labels == null) return null;
        List<SaveStateType> values = new ArrayList<>(labels.size());
        for (String label : labels) values.add(fromPg(label));
        return values;
    }

    @Nullable
    public static List<String> pgLabels(@Nullable List<SaveStateType> values) {
        if (values == null) return null;
        List<String> labels = new ArrayList<>(values.size());
        for (SaveStateType value : values) labels.add(value == null ? null : value.pgLabel);
        return labels;
    }
}
