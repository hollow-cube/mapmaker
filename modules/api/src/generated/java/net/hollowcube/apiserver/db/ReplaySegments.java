// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.ResultSet;
import java.sql.SQLException;
import org.jetbrains.annotations.Nullable;

/**
 * A row of the `replay_segments` table.
 */
public record ReplaySegments(String replayId, long segmentIndex, @Nullable String objectReference,
        @Nullable byte[] data, long length, byte[] digest, long commitRevision) {
    /**
     * Reads one `replay_segments` row, its first column at `col`.
     */
    static ReplaySegments read(ResultSet rs, int col) throws SQLException {
        return new ReplaySegments(
                rs.getString(col),
                rs.getLong(col + 1),
                rs.getString(col + 2),
                rs.getBytes(col + 3),
                rs.getLong(col + 4),
                rs.getBytes(col + 5),
                rs.getLong(col + 6));
    }
}
