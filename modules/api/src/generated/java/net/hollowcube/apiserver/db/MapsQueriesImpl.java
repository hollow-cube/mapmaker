// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.UUID;
import net.hollowcube.sqlgen.runtime.ConnectionSource;
import net.hollowcube.sqlgen.runtime.Sneaky;

/**
 * Runs MapsQueries against a connection borrowed per statement.
 */
@SuppressWarnings("UnusedReturnValue")
final class MapsQueriesImpl implements MapsQueries {
    private static final String IS_MAP_PUBLISHED = """
        -- Whether `[map]` in a message is a map the people reading it could open. Matches what a map server
        -- used to answer for itself out of `MapData.isPublished`.
        select count(*) > 0 as published
        from maps
        where id = ?
          and published_id is not null""";

    private final ConnectionSource source;

    MapsQueriesImpl(ConnectionSource source) {
        this.source = source;
    }

    @Override
    public boolean isMapPublished(UUID mapId) {
        try {
            Connection conn = source.acquire();
            try (PreparedStatement ps = conn.prepareStatement(IS_MAP_PUBLISHED)) {
                ps.setObject(1, mapId);
                try (ResultSet rs = ps.executeQuery()) {
                    if (!rs.next()) throw new SQLException("isMapPublished returned no row");
                    return rs.getBoolean(1);
                }
            } finally {
                source.release(conn);
            }
        } catch (SQLException e) {
            throw Sneaky.rethrow(e);
        }
    }
}
