// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.time.Instant;
import net.hollowcube.sqlgen.runtime.Jdbc;
import org.jetbrains.annotations.Nullable;

/**
 * A row of the `jobs` table.
 */
public record Jobs(String job, String instance, @Nullable String data, @Nullable Instant runAt,
        @Nullable String pickedBy, @Nullable Instant pickedAt, @Nullable Instant heartbeat,
        int attempts, @Nullable String lastError, @Nullable Instant lastSuccess,
        @Nullable Instant parkedAt, long version) {
    /**
     * Reads one `jobs` row, its first column at `col`.
     */
    static Jobs read(ResultSet rs, int col) throws SQLException {
        return new Jobs(
                rs.getString(col),
                rs.getString(col + 1),
                rs.getString(col + 2),
                Jdbc.getInstant(rs, col + 3),
                rs.getString(col + 4),
                Jdbc.getInstant(rs, col + 5),
                Jdbc.getInstant(rs, col + 6),
                rs.getInt(col + 7),
                rs.getString(col + 8),
                Jdbc.getInstant(rs, col + 9),
                Jdbc.getInstant(rs, col + 10),
                rs.getLong(col + 11));
    }
}
