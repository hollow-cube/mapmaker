// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.time.Instant;
import net.hollowcube.sqlgen.runtime.Jdbc;
import org.jetbrains.annotations.Nullable;

/**
 * A row of the `punishments` table.
 */
public record Punishments(int id, String playerId, String executorId, String type,
        Instant createdAt, @Nullable String ladderId, String comment, @Nullable Instant expiresAt,
        @Nullable String revokedBy, @Nullable Instant revokedAt, @Nullable String revokedReason) {
    /**
     * Reads one `punishments` row, its first column at `col`.
     */
    static Punishments read(ResultSet rs, int col) throws SQLException {
        return new Punishments(
                rs.getInt(col),
                rs.getString(col + 1),
                rs.getString(col + 2),
                rs.getString(col + 3),
                Jdbc.getInstant(rs, col + 4),
                rs.getString(col + 5),
                rs.getString(col + 6),
                Jdbc.getInstant(rs, col + 7),
                rs.getString(col + 8),
                Jdbc.getInstant(rs, col + 9),
                rs.getString(col + 10));
    }
}
