// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.time.Instant;
import java.util.UUID;
import net.hollowcube.sqlgen.runtime.Jdbc;

/**
 * A row of the `map_slots` table.
 */
public record MapSlots(UUID playerId, UUID mapId, int index, Instant createdAt, boolean isPending) {
    /**
     * Reads one `map_slots` row, its first column at `col`.
     */
    static MapSlots read(ResultSet rs, int col) throws SQLException {
        return new MapSlots(
                rs.getObject(col, UUID.class),
                rs.getObject(col + 1, UUID.class),
                rs.getInt(col + 2),
                Jdbc.getInstant(rs, col + 3),
                rs.getBoolean(col + 4));
    }
}
