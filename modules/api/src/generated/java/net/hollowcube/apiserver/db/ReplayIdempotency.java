// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.time.Instant;
import net.hollowcube.sqlgen.runtime.Jdbc;

/**
 * A row of the `replay_idempotency` table.
 */
public record ReplayIdempotency(String replayId, String idempotencyKey, byte[] requestFingerprint,
        int responseStatus, String responseEtag, String responseMetadata, Instant createdAt) {
    /**
     * Reads one `replay_idempotency` row, its first column at `col`.
     */
    static ReplayIdempotency read(ResultSet rs, int col) throws SQLException {
        return new ReplayIdempotency(
                rs.getString(col),
                rs.getString(col + 1),
                rs.getBytes(col + 2),
                rs.getInt(col + 3),
                rs.getString(col + 4),
                rs.getString(col + 5),
                Jdbc.getInstant(rs, col + 6));
    }
}
