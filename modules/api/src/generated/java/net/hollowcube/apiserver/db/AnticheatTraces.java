// Generated by sql-gen from src/main/sql. Do not edit.
package net.hollowcube.apiserver.db;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.time.Instant;
import java.util.UUID;
import net.hollowcube.sqlgen.runtime.Jdbc;
import org.jetbrains.annotations.Nullable;

/**
 * A row of the `anticheat_traces` table.
 */
public record AnticheatTraces(String id, @Nullable String captureId, UUID playerId,
        @Nullable String proxyVersion, @Nullable String proxy, int clientPvn,
        @Nullable String reason, int formatVersion, Instant startedAt, @Nullable Instant endedAt,
        long bytes, String path, boolean pinned, @Nullable Instant expiresAt, Instant createdAt) {
    /**
     * Reads one `anticheat_traces` row, its first column at `col`.
     */
    static AnticheatTraces read(ResultSet rs, int col) throws SQLException {
        return new AnticheatTraces(
                rs.getString(col),
                rs.getString(col + 1),
                rs.getObject(col + 2, UUID.class),
                rs.getString(col + 3),
                rs.getString(col + 4),
                rs.getInt(col + 5),
                rs.getString(col + 6),
                rs.getInt(col + 7),
                Jdbc.getInstant(rs, col + 8),
                Jdbc.getInstant(rs, col + 9),
                rs.getLong(col + 10),
                rs.getString(col + 11),
                rs.getBoolean(col + 12),
                Jdbc.getInstant(rs, col + 13),
                Jdbc.getInstant(rs, col + 14));
    }
}
