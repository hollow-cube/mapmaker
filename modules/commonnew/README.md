# Common

New common module, will represent actually common bits which will be present outside of just mapmaker (eventually probably to be migrated to public common repo).

The current common module will be turned into `core` and will be used for the core mapmaker functionality.