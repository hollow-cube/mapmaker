# Block Entities in Terraform

Currently, we have a default set of blocks which can be overridden to have block entities (handlers or nbt).

In palettes the blocks are stored separately if they have a block entity which seems a little yikes.

It would be nice if palettes could care only about blocks with different handlers from the default set or something.
