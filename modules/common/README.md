# common

A common module for all modules present in this project.

This module is different than `core` because this does _not_ contain any mapmaker specific content.
This module may be moved to the common repository and merged with that common one eventually.
