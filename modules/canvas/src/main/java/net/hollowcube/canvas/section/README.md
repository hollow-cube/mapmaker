# Canvas Section API

Low level API for creating GUIs (aka v1), provides the core layer for the higher level xml/view api on top.

Eventually will probably be removed in favor of a stripped down version of RouterSection for the xml/view api.\
